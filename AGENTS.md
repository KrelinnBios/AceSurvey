# AGENTS.md

本文件适用于整个仓库，作为 AI 编码代理修改本项目时的项目规范。除非用户另有要求，使用中文回复和说明。

## 项目概览

AceSurvey 是一个面向无性恋社群的静态问卷与卡片生成工具。

- 项目没有构建步骤，页面由原生 HTML、CSS 和 JavaScript 组成。
- `index.html` 是问卷填写、草稿保存、卡片预览和图片导出的唯一入口。
- 网站可直接打开，也可部署到任意静态托管平台。
- 页面内容用于社群问卷填写、自我梳理和身份表达辅助，不构成医疗、心理、法律或其他专业建议。
- 项目不提供远程提交接口，访问者填写和导出的内容应继续留在本地处理。

## 开始任务前

- 先读取与任务直接相关的 HTML、CSS 和 JavaScript，确认现有数据流、草稿逻辑和导出流程后再修改。
- 优先做最小、可验证的改动，不把局部需求扩展成无关重构、整页格式化或技术栈迁移。
- 涉及问卷题项、存储字段或导出内容时，先检查填写、恢复、重置、预览和导出之间是否共享同一份数据。

## 修改原则

- 文件统一使用 UTF-8，避免 PowerShell 默认编码造成中文乱码。
- 保持项目为可独立部署的静态页面，不引入必须依赖后端或构建流程的功能。
- 修改问卷题项、草稿保存、卡片预览或导出逻辑时，同时检查移动端浏览器和 QQ 内置浏览器的兼容性与体验。
- 完整字体与图片导出体验依赖 Google Fonts 和 html2canvas CDN；验证导出时需要网络可用。
- 不把问卷问题或导出结果描述为诊断、医学结论、专业评估或价值判断。
- Git diff 只包含本次任务所需内容，不混入批量格式化、临时导出文件或其他无关修改。

## 关键约束

### 静态资源版本号

修改以下任一文件后，必须同步更新 `index.html` 中对应的 `?v=` 版本号：

- `assets/theme.css`
- `assets/theme.js`
- `icon/logo.svg`

三处引用必须使用同一个新版本号，推荐格式为 `YYYYMMDDX`，例如 `202609081`：

```text
icon/logo.svg?v=<新版号>
assets/theme.css?v=<新版号>
assets/theme.js?v=<新版号>
```

只修改 HTML 内容本身时，不需要更新版本号。

#### 原因说明

- HTML 已通过缓存控制设置为不缓存，但 CSS、JavaScript 和图片仍可能被浏览器或 CDN 缓存。
- 只有资源 URL 变化时，浏览器才会可靠地重新获取新资源。
- 漏改版本号会造成线上继续加载旧样式或旧脚本，与仓库当前代码不一致。

### 内容与数据边界

- 用户填写的答案、草稿和导出内容应继续在本地处理，不新增远程提交、追踪或隐式上传。
- 不把任何题项、分数或结果扩展为临床诊断、医学判断、专业评估或价值排序。
- 修改隐私相关行为时，必须同步检查页面说明和 README，确保实际处理方式与公开描述一致。

## 验证

根据改动范围人工确认：

- `index.html` 可直接在浏览器打开，无新增脚本错误。
- 问卷填写、刷新后的草稿恢复、重置和导出流程正常。
- 卡片预览和导出的 PNG 排版无明显错位。
- 窄屏下输入框、滚动、预览和长按保存体验可用。
- 外链字体或 html2canvas 加载失败时，页面仍保留基本可用性。
- 修改共享资源后，三处版本号已同步更新。

本项目没有构建步骤或自动化测试套件，对用户可见的改动以浏览器实际检查为主要验证方式。

## 文档同步

- `README.md` 面向项目使用者；`AGENTS.md` 只记录编码代理在修改项目时需要遵守的边界和维护规则。
- README 主体结构固定为：项目简介 → 功能概览 → 界面预览 → 使用方式 → 隐私与数据 → 内容边界 → 许可协议 → 反馈与贡献。
- 项目简介结尾保留范围或用途说明，但不要和“隐私与数据”重复表述。
- 功能概览使用扁平列表，格式固定为 `- 四字标签：描述。`；标签不加粗且正好四个汉字。
- 顶部 logo 统一使用仓库根目录下的 `icon/logo.svg`。
- 修改公开功能、隐私处理、内容边界或资源路径时，检查 README 是否需要同步。

## 提交约定

- 一个提交聚焦一个明确主题。
- 提交信息简短说明实际结果。
- 不提交本地预览、编辑器状态、临时导出或与任务无关的生成文件。
- 工作区已有其他修改时，只处理并提交本次任务涉及的内容。