# AceSurvey 项目维护指南

本文档面向仓库维护者与自动化编码代理，记录 AceSurvey 项目特有的修改流程、代码风格与发布规范。

---

## 一、缓存与静态资源版本号更新流程（重要）

Cloudflare Pages 等静态托管平台和浏览器会对 CSS、JS、图片等本地资源执行强缓存。为避免用户访问时仍使用旧版资源，**每次修改以下任一文件后，都必须同步更新 index.html 中对应的版本号**。

### 需同步更新版本号的文件

- `assets/theme.css`
- `assets/theme.js`
- `icon/logo.svg`

> 仅修改 HTML 内容时，不需要更新版本号。

### 操作步骤

1. 完成代码改动后，打开 `index.html` 的 `<head>` 区块。
2. 查找以下 3 处 `?v=` 后缀，将版本号字符串统一更新为同一个新值：

   ```
   icon/logo.svg?v=<新版号>
   assets/theme.css?v=<新版号>
   assets/theme.js?v=<新版号>
   ```

3. 新版号推荐格式：`YYYYMMDDX`（日期 + 序号，例如 `202609081`）。
4. 3 处版本号必须保持完全一致。
5. 确认无误后，再执行 `git commit / push`。

### 原因说明

- HTML 通过 `<meta http-equiv="Cache-Control">` 已设为不缓存，但 CSS / JS / 图片仍会被浏览器缓存。
- 只有资源 URL 发生变化（即 query string `?v=` 取值不同）时，浏览器才会强制重新下载对应资源。
- 漏改版本号会导致用户访问站点时仍加载旧的 CSS / JS 效果，出现与本地修改后线上不一致的问题。

---

## 二、项目定位

AceSurvey 是一个面向无性恋社群的静态问卷与卡片生成工具。

- 项目没有构建步骤，页面由原生 HTML、CSS 和 JavaScript 组成。
- `index.html` 是问卷填写、草稿保存、卡片预览和图片导出的唯一入口。
- 网站可直接打开，也可部署到任意静态托管平台。
- 页面内容用于社群问卷填写、自我梳理和身份表达辅助，不构成医疗、心理、法律或其他专业建议。
- 项目不提供远程提交接口，访问者填写和导出的内容应继续留在本地处理。

---

## 三、修改原则

1. 文件统一使用 UTF-8 编码保存，避免因 PowerShell 默认编码造成中文乱码。
2. 保持项目为可独立部署的静态页面，不引入必须依赖后端或构建流程的功能。
3. 修改问卷题项、草稿保存、卡片预览或导出逻辑时，同步检查移动端浏览器和 QQ 内置浏览器的兼容性与体验。
4. 完整字体与图片导出体验依赖 Google Fonts 和 html2canvas CDN；验证导出时需要网络可用。
5. 不把问卷问题或导出结果描述为诊断、医学结论、专业评估或价值判断。
6. Git diff 应只包含本次任务所需内容，避免混入批量格式化或临时导出文件。

---

## 四、README 约定

- 结构固定为：项目简介 → 功能概览 → 界面预览 → 使用方式 → 隐私与数据 → 内容边界 → 许可协议 → 反馈与贡献，与本作者其他仓库保持一致。
- 项目简介结尾补一句范围或用途说明，不要和后面「隐私与数据」小节的具体表述重复。
- 功能概览的每条格式固定为 `- 四字标签：描述。`，不加粗；标签需正好四字，不要三字或五字；条目较多时保持扁平列表，不要按类别拆出三级标题。
- 顶部 logo 统一放在 `icon/logo.svg`（相对仓库根目录），与本作者其他仓库保持一致。

---

## 五、提交前检查清单

根据改动范围，提交前人工确认以下对应项目：

- `index.html` 可直接在浏览器打开，无脚本错误。
- 问卷填写、刷新后的草稿恢复、重置和导出流程正常。
- 卡片预览和导出的 PNG 排版无明显错位。
- 窄屏下输入框、滚动、预览和长按保存体验可用。
- 外链字体或 html2canvas 加载失败时，页面仍保留基本可用性。
